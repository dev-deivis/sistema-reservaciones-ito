PORT=3001

DB_HOST=localhost
DB_PORT=5432
DB_NAME=reservaciones_ito
# Debe coincidir con las credenciales del backend principal (backend/.env)
DB_USER=admin
DB_PASSWORD=

# Clave interna para proteger el endpoint POST /api/notificaciones
# Debe coincidir con INTERNAL_API_KEY del backend principal
INTERNAL_API_KEY=secret_key_interna
